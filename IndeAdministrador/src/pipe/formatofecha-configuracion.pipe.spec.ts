import { FormatofechaConfiguracionPipe } from './formatofecha-configuracion.pipe';

describe('FormatofechaConfiguracionPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatofechaConfiguracionPipe();
    expect(pipe).toBeTruthy();
  });
});
